<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta http-equiv="X-UA-Compatible" content="IE=edge">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Listado de Personajes de Harry Potter</title>
</head>
<body>
<div class="text-center">
<h1>Edicion de Personajes de Harry Potter</h1>
</div>
<div class="card">
  <div class="card-header">
    <ul class="nav nav-tabs card-header-tabs">
      <li class="nav-item">
        <a class="nav-link active" aria-current="characters" href="/">List</a>
      </li>
      <li class="nav-item">
        <%= link_to "New character", new_character_path, class:"nav-link" %>
      </li>
      <li class="nav-item">
      <%= link_to "Edit to characters", characters_path, class:"nav-link"%>
    </li>
    </ul>
  </div>
  </div>
</div>

<p style="color: green"><%= notice %></p>

<h1>Characters</h1>

<div id="characters">
  <% @characters.each do |character| %>
    <%= render character %>
    <p>
      <%= link_to "Show this character", character %>
    </p>
  <% end %>
</div>

<%= link_to "New character", new_character_path %>


